<!--投稿フォームの作成-->
<!--form_withは、文字を入力するboxを作成するイメージ-->
<!--モデルとの連携、どのurlへデータを送るのか、どのHTTPメソッドで送るかなどを定義できる-->
<!--ブロック変数「f」にはmodelの情報が含まれている-->
<h1>新規投稿</h1>
<%= form_with model: @list, url: '/lists', method: :post do |f| %>

<h4>タイトル</h4>
<%= f.text_field :title %>

<!--「f」にはListモデルの情報が入っているため、このプロパティ名をtitileとすることで-->
<!--ListモデルにあるtitleカラムとRailsは判別することができ、データベースに保存することができる-->
<!--単一行の場合は、text_fieldを使用 ：titleは保存先を指定。titleに保存される-->

<h4>本文</h4>
<%= f.text_area :body %>

<%= f.submit '投稿' %>

<% end %>
